<project>
  <modelVersion>4.0.0</modelVersion>
    
  <!-- project settings -->
  <groupId>net.meisen.tools</groupId>
  <artifactId>net-meisen-tools-ant-processenabler-gwt</artifactId>
  <version>${gwt.version}</version>
  <name>Standard GWT compiler dependencies</name>
  
  <dependencies>
    <!-- the GWT compiler -->
    <dependency>
      <groupId>com.google.gwt</groupId>
      <artifactId>gwt-dev</artifactId>
      <version>${gwt.version}</version>
    </dependency>
    
    <!-- not always needed, but added by default -->
    <dependency>
      <groupId>com.google.gwt</groupId>
      <artifactId>gwt-user</artifactId>
      <version>${gwt.version}</version>
    </dependency>
  </dependencies>
</project>